## 단축 명령

### 오늘의 공부!

사용자가 정확히 `오늘의 공부!`라고 입력하면 추가 질문 없이 다음 작업을 수행한다.

1. 현재 날짜를 확인한다.
2. `CURRICULUM.md`에서 다음 학습 위치를 확인한다.
3. `.sources`의 기준 레포에서 아직 학습하지 않은 실제 면접 질문을 선택한다.
4. 다음 학습 노트 **한 개**를 생성한다 — **AI·DA 질문 1개**.
   - Backend 트랙은 **중단됨**. 재개 전까지 Backend 노트를 만들지 않는다. 자세한 내용은 `CURRICULUM.md` 참고.
5. `templates/daily-question.md` 형식을 사용한다.
6. 구현이나 긴 코드 작성이 필요한 질문은 제외한다.
7. 사용자 작성 영역인 `내가 정리한 내용`과 `검토 결과`는 비워 둔다.
8. Git 커밋과 Push는 수행하지 않는다.
9. 이미 오늘 날짜의 노트가 있다면 덮어쓰지 말고 기존 노트를 열어 학습을 이어간다.
10. 생성 또는 확인한 노트의 경로와 질문 제목만 간단히 안내한다.
11. 안내 후에는 파일이 아니라 대화 공간에서 학습을 이어간다. 세부 규칙은 `학습 진행 방식`을 따른다.

생성 파일 형식:

- `notes/YYYY/YYYY-MM-DD-ai-data-topic.md`
- `notes/YYYY/YYYY-MM-DD-backend-topic.md` — **Backend 재개 시에만**

### 공부 완료!

사용자가 정확히 `공부 완료!`라고 입력하면 추가 질문 없이 다음 작업을 수행한다.

1. 오늘 날짜의 AI·DA 노트가 존재하는지 확인한다. (Backend 재개 시에는 Backend 노트도 함께 확인한다.)
2. 노트의 학습 내용이 작성돼 있는지 확인한다.
3. **노트 날짜와 오늘 날짜가 다르면 아래 `날짜 불일치 확인`을 먼저 수행한다.**
4. 노트의 `status`를 `completed`로 변경한다.
5. `CURRICULUM.md`의 진행 위치를 갱신한다.
6. 아래 스크립트를 실행한다. 노트 날짜가 오늘이 아니면 `-Date YYYY-MM-DD`로 노트 날짜를 넘긴다.

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\finish-day.ps1
```

### 날짜 불일치 확인

노트 생성일과 실제 학습일이 다른 경우가 있다. 노트의 `date`와 오늘 날짜가 다르면 **완료 처리 전에 사용자에게 어느 날짜로 기록할지 묻는다.**

1. 두 날짜를 함께 제시한다 — 노트 날짜, 오늘 날짜.
2. 각 선택이 **스트릭에 어떤 영향**을 주는지 미리 계산해 알려준다(현재 스트릭·최장 스트릭 변화).
3. 사용자가 오늘 날짜를 택하면 다음을 모두 수행한 뒤 완료 처리한다.
   - 노트 파일명을 `YYYY-MM-DD-...` 로 변경한다.
   - 노트 frontmatter의 `date`를 수정한다.
4. 사용자가 노트 날짜를 유지하면 `finish-day.ps1`에 `-Date`로 그 날짜를 넘긴다.

**기준**: 노트 날짜는 파일을 만든 날이 아니라 **실제로 학습한 날**이어야 한다. 설명을 읽고 답변하고 정리한 날이 기준이다.

## 학습 진행 방식

노트 파일은 **기록용**이고, 실제 학습은 **대화 공간에서** 진행한다.

1. 노트는 한국어 기준으로 깔끔하게 작성한다. 질문 원문(영어)은 출처 확인용으로만 병기한다.
2. 노트 생성 직후 대화에서 첫 질문 설명을 시작한다. 파일에 학습 과정을 미리 다 적어 두지 않는다.
3. 설명 → 질의응답 → 사용자가 자기 언어로 정리 순서로 진행한다.
4. `내가 정리한 내용`은 사용자가 대화에서 정리한 내용이 나온 뒤에만 채운다.
5. `검토 결과`는 그 정리에 대한 피드백을 대화에서 주고받은 뒤에 채운다.
6. 두 섹션을 사용자 동의 없이 미리 채우지 않는다.

### 질문 선택

1. `CURRICULUM.md`의 `현재 위치`에서 이어지는 **다음 질문**을 고른다.
2. **이미 학습한 주제와 겹치면 건너뛴다.** 기준 레포마다 비슷한 질문이 있어 중복이 자주 생긴다.
   - 건너뛴 경우 노트 상단에 `> ℹ️` 로 **사유를 기록**한다.
     예: `> ℹ️ Sample Questions 3·4번은 [[2026-07-20-...]]에서 학습해 건너뜀.`
   - `CURRICULUM.md`의 `현재 위치`에도 어느 항목을 건너뛰었는지 남긴다.
3. **트랙 간 중복도 피한다.** 예를 들어 Decision trees를 ML 트랙에서 다뤘다면 DA 트랙은 다음 주제로 넘어간다.

### 기준 레포에 답변이 없을 때

레포에 따라 **질문만 있고 답변이 없거나**(`alirezadir/Machine-Learning-Interviews`), **외부 링크로 대체**(`amitshekhariitbhu/...`)된 경우가 있다.

이때 `기준 답변` 섹션은 표준 교재 수준으로 재구성하되, **맨 위에 출처 한계를 명시**한다.

```markdown
> ⚠️ 이 기준 레포는 질문 목록만 제공하고 답변은 싣지 않는다.
> 아래는 표준적인 ML 교재 기준으로 재구성한 답변이다.
```

## 노트 구조

`templates/daily-question.md`를 따르되, `검토 결과`는 아래 하위 섹션으로 구성한다.

| 섹션 | 내용 | 필수 |
|---|---|---|
| `### 확인된 부분` | 사용자가 정확히 짚은 것. 무엇을 잘했는지 구체적으로 | ✅ |
| `### 보강 포인트` | 틀렸거나 빠진 것. **교정 전/후를 함께** 적어 나중에 봐도 맥락이 남게 | ✅ |
| `### 심화: <주제>` | 대화에서 깊이 파고든 내용. 여러 개 가능 | 선택 |
| `### 이전 학습과의 연결` | 다른 노트와의 관계를 `[[파일명]]` 링크로 | 권장 |
| `### 오늘 나온 축약어` | 축약어–풀네임 표 | 권장 |

**노트 간 링크**는 확장자 없는 파일명으로 쓴다 — `[[2026-08-06-ai-data-self-attention]]`. Obsidian 자산이라 그래프 뷰로 연결이 보인다.

## 학습 깊이

**깊이를 우선한다.** 설명에 표·비유·구체적 수치를 쓰고, 사용자 답변에는 **틀린 곳 교정 + 한 겹 더 파는 심화**를 붙인다. 노트가 길어져도 무방하다.

- 사용자가 정리를 제출하면 **교정 제안을 먼저 보여주고 동의를 받은 뒤** 노트에 반영한다.
- 답변이 맞아도 **"왜 그런가"를 한 겹 더** 물어 원리까지 닿게 한다.

### 질문 규칙

대화에서 사용자에게 질문을 던질 때 다음 세 가지를 지킨다.

#### 1. 개념 위주로 낸다

**수식이 아니라 개념을 묻는다.**

- ❌ "목적 함수 수식을 보고 왜 `C`와 `λ`의 방향이 반대인지 설명하라"
- ✅ "`C`를 키우면 왜 과적합 쪽으로 가는가? 모델이 무엇을 더 중요하게 여기게 되는가"

**기준**: 수식을 읽어야만 풀리는 질문은 내지 않는다. 수식을 몰라도 **원리를 이해했다면 답할 수 있는** 질문을 낸다.

- 설명(`핵심 개념`·`기준 답변`)과 노트에는 수식을 실어도 된다. **제한 대상은 질문뿐이다.**
- 수식이 개념 이해를 돕는 자리에서는 **설명하는 쪽에서 먼저 풀어 주고**, 질문은 그 의미를 묻는다.
- 면접 답변도 수식 암송이 아니라 **말로 설명하는 것**이므로 이 방식이 실전에 가깝다.

#### 2. 한 번에 최대 3개까지만 낸다

한꺼번에 많이 던지지 않는다. **서술형 질문과 퀴즈를 합쳐 3개 이하**로 제한한다.

- 개수가 많으면 답이 짧아지고 깊이가 얕아진다.
- 더 파야 할 게 남았으면 **다음 턴에 이어서** 묻는다.

**3개를 넘겨 물어야 할 때는 객관식으로 낸다.**

- 서술형은 답을 쓰는 부담이 크지만, 객관식은 고르기만 하면 되므로 개수를 늘려도 된다.
- 넓게 훑어야 하는 자리(용어 확인, 개념 구분, 상황별 선택)에 적합하다.
- 객관식이라도 **답은 직전 설명 안에 있어야 한다**(아래 3번).

```markdown
**Q. 다음 중 LRU가 보는 기준은?**
① 캐시에 넣은 시각  ② 마지막으로 조회한 시각  ③ 항목의 크기  ④ 조회 횟수
```

#### 3. 답이 바로 앞 설명 안에 있어야 한다

**직전에 한 설명만 읽고도 답할 수 있는 질문만 낸다.**

- ❌ 설명에 없던 내용을 "생각해 보라"고 묻기 — 그건 질문이 아니라 퀴즈다.
- ❌ 이전 노트를 다시 열어봐야 답이 나오는 질문.
- ✅ 방금 설명한 개념을 **적용·연결·재구성**하게 하는 질문.

**이전 학습과 연결하고 싶으면**, 필요한 내용을 질문 안에 **먼저 요약해 준 뒤** 묻는다.

- ❌ "어제 배운 함정 하나가 여기에도 적용되는데 뭘까?"
- ✅ "어제 `C` 같은 파라미터는 로그 스케일로 훑어야 한다고 했다. 커널 SVM의 `γ`에도 같은 게 적용될까?"

## 표기 규칙

### 축약어

축약어는 **처음 등장할 때 반드시 풀네임을 함께 표기**한다. 노트와 대화 모두에 적용한다.

- 형식: `축약어(Full Name)` — 예: `CDN(Content Delivery Network)`
- 한글 설명이 필요하면 뒤에 덧붙인다 — 예: `TTL(Time To Live, 캐시 유효기간)`
- 같은 문서 안에서 두 번째부터는 축약어만 써도 된다.
- 노트에서는 제목·`핵심 개념`·`기준 답변` 등 **섹션이 바뀌어 처음 나오는 자리**에도 풀네임을 병기해 두면 복습에 좋다.
